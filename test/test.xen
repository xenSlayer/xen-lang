# Compute the x'th fibonacci number.
def fib(x)
  if x < 3 then
    return 1
  else
    fib(x-1)+fib(x-2)

# This expression will compute the 40th number.
fib(40)

# expressions #
## keywords ##
# def, return, then, if, else
## identifier ##
# fib, x
## operators ##
# <, +, -
